So yesterday I started with the nav2 block , and i achieved some stuff i used twist-mux , and i solved every bug i encountered , but then it started working but it crashed , and ubuntu kept seding amcl app failed , like maybe the laptop im using is weak , but i dont know i ll keep working , so the LIMO Nav2 + twist_mux setup. `twist_mux` is working and manual teleop through `/cmd_vel_manual` already moves the robot in Gazebo using `-r /cmd_vel_out:=/cmd_vel`. The remaining blocker is that default localization works, but localization with my custom `~/limo_ros2_ws/config/nav2_params.yaml` is still unstable after motion even after fixing `robot_model_type` to `nav2_amcl::DifferentialMotionModel`. so we need to compare the custom AMCL params against the working default localization and stabilize that before we resume full navigation through `/cmd_vel_nav`.
